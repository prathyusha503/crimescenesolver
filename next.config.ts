import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;

// next.config.ts

// import type { NextConfig } from 'next';

// const nextConfig: NextConfig = {
//   experimental: {
//     serverActions: {
//       bodySizeLimit: '200mb', // Increase file size limit
//     },
//   },
// };

// export default nextConfig;
